name: Build branch

on:
  release:
    types: [published]
  push:
    branches:
      - master
    paths-ignore:
      - "**.md"
      - "**.yml"
  workflow_dispatch:

jobs:
  build_esp32:
    runs-on: ubuntu-latest
    #runs-on: ubuntu-24.04 - p4 stream

    strategy:
      fail-fast: false
      matrix:
        environment:
          - out: elecrow
            env: "esp2-p4-c6-crowpanel-advances-10.1i_hmi"
          - out: waveshare
            env: "esp32-one_ili9486 -e esp32-one_st7796 -e ws_esp32_s3_touch_lcd_4p3 -e ws_esp32_s3_touch_lcd_4 -e waveshare-esp32-s3-touch-lcd-7 -e waveshare-esp32-s3-touch-lcd-7-gpio16"


    steps:
      - uses: actions/checkout@v6
        with:
          submodules: "true"
      - name: Cache pip
        uses: actions/cache@v5
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-
      - name: Cache PlatformIO
        uses: actions/cache@v5
        with:
          path: ~/.platformio
          key: ${{ runner.os }}-${{ hashFiles('**/lockfiles') }}
      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          # python-version: "3.x"
          python-version: "3.12" # Stable version as of 2024-06
      - name: Install PlatformIO & Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install --upgrade platformio dulwich
      #- name: Install Setuptools
      #  run: |
      #    pip install --upgrade setuptools # obsolete, use importlib.metadata instead
      - name: Enable ESP32 platforms from platformio_override-template.ini
        run: |
          sed 's/; user_setups\/esp32/user_setups\/esp32/g' platformio_override-template.ini > platformio_override.ini
      - name: List all files in current folder
        run: |
          ls -la
      - name: Cat platformio_override.ini
        run: |
          cat platformio_override.ini
      - name: Run PlatformIO
        run: pio run -e ${{ matrix.environment.env }}
      - name: Upload output file
        uses: actions/upload-artifact@v6
        with:
          name: ${{ matrix.environment.out }}
          path: build_output/firmware/*.*n
      ## Release only:
      - name: Zip artifact for deployment
        if: github.ref != 'refs/heads/master'
        run: |
          zip ${{ matrix.environment.out }}.zip build_output/firmware/*.bin -r
      - name: On Release, upload assets to that Release
        if: github.ref != 'refs/heads/master'
        uses: AButler/upload-release-assets@v3.0
        with:
          files: ${{ matrix.environment.out }}.zip
          repo-token: ${{ secrets.GITHUB_TOKEN }}


  linux_build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          submodules: "true"
      - name: Cache pip
        uses: actions/cache@v5
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-
      - name: Cache PlatformIO
        uses: actions/cache@v5
        with:
          path: ~/.platformio
          key: ${{ runner.os }}-${{ hashFiles('**/lockfiles') }}
      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          # python-version: "3.x"
          python-version: "3.12" # Stable version as of 2024-06
      - name: Install PlatformIO & Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install --upgrade platformio dulwich
      - name: Enable Linux platform from platformio_override.ini
        run: |
          sed 's/; user_setups\/linux/user_setups\/linux/g' platformio_override-template.ini > platformio_override.ini
          mkdir -p .pio/libdeps/linux_sdl/paho/src
      - name: Install SDL2 library
        run: |
          sudo apt-get update
          sudo apt-get install libsdl2-dev
      - name: List all files in current folder
        run: |
          ls -la
      - name: Cat platformio_override.ini
        run: |
          cat platformio_override.ini
      - name: Enable Linux platform from platformio_override.ini
        run: |
          sed -i 's/; user_setups\/linux/user_setups\/linux/g' platformio_override.ini
          mkdir -p .pio/libdeps/linux_sdl/paho/src
      - name: Run PlatformIO
        run: pio run -e linux_sdl -e linux_headless -e linux_fbdev
